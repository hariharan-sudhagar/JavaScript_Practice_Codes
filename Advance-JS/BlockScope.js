// IT is a scope range of let and consts

// i already discussed about Block scope in Let&const lesson

// block scope is is range where variables can be accessable.. withhin that range only the varibles are acceable in general they're in block 

//block means {    } if a var is decalared by let or const that can be accessable within that block.



if(true){
     // Copund Statement
     var a =10;
     console.log(a);
}